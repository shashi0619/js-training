//WAP to find the maximum of two numbers

const readline = require('readline-sync');

const num1 = readline.question("Enter the first number:");
const num2 = readline.question("Enter the second number:");


{
if (num1 >= num2)
    

    console.log ( num1 + "  number is maximum "  );
 
if (num1 < num2)

    console.log ( num2 + "  number is maximum "  );
}


console.log (" end of if  ")
